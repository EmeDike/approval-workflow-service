spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/approval_workflow
    username: approval_user
    password: approval_pass
    driver-class-name: org.postgresql.Driver

logging:
  level:
    org.hibernate.SQL: DEBUG